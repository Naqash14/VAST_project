[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "postgresql.lib"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["pip install psycopg2-binary"]

[start]
cmd = "gunicorn run:app --bind 0.0.0.0:$PORT"
